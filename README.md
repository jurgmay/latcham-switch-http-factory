# switch-http-factory

A lightweight, retry-enabled HTTP client used in Latcham’s Enfocus Switch scripts and Node-based automations.

## Features

-   Axios-based with automatic retries
-   Simple wrapper methods (`get`, `post`, `put`, `delete`)
-   Friendly error messages
-   Strong TypeScript types
-   Designed for reusable Switch scripting utilities

## Installation

From GitHub:

```bash
npm install github:latcham/latcham-http-client
```
